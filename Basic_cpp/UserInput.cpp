#include<bits/stdc++.h> //shortcut that allows you to include almost all standard libraries at once using
#include<iostream>
using  namespace std;

int main() {
    std::cout << "Hey, Towhid!";
    std::cout << '\n';
    cout << "Rakibul...!" << endl; // if -> using namespace std; no need to use std::

    // taking user input
    int x;
    cin >> x;
    cout << "Valuse of x: " << x;
    return 0;
}