#include<bits/stdc++.h>
using namespace std;

int main() {
    // everything same as map, only it can store multiple keys, duplicate keys
    // only mpp[key] cannot be used here
    // everything will be sorted order
}